providers = ["python"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.miniapp_build]
dependsOn = ["setup"]
cmds = ["cd miniapp && npm install && npm run build"]

[phases.install]
dependsOn = ["miniapp_build"]

[start]
cmd = "bash start.sh"
