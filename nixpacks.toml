providers = ["python"]

[phases.setup]
nixPkgs = ["nodejs_20", "python311", "gcc"]

[phases.miniapp_build]
dependsOn = ["setup"]
cmds = ["cd miniapp && npm ci && npm run build"]

[phases.install]
dependsOn = ["miniapp_build"]

[start]
cmd = "bash start.sh"
