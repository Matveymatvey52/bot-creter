providers = ["python"]

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.miniapp_build]
dependsOn = ["setup"]
cmds = ["npm install -g npm@latest && cd miniapp && npm ci && npm run build"]

[phases.install]
dependsOn = ["miniapp_build"]

[start]
cmd = "bash start.sh"
